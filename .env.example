# Environment variables template
# Copy this to .env and fill in your values

# Flask configuration
FLASK_ENV=production
SECRET_KEY=your-secret-key-here-change-this
JWT_SECRET_KEY=your-jwt-secret-key-here-change-this

# Generate secure keys with:
# python -c "import secrets; print(secrets.token_hex(32))"

# Database (optional - defaults to SQLite)
# For Vercel, use Vercel Postgres or external database
# DATABASE_URL=postgresql://user:password@host:port/database

# Audiveris (optional - not available on Vercel serverless)
# AUDIVERIS_PATH=/opt/audiveris

# Server configuration
PORT=5000

# Vercel-specific notes:
# - Set environment variables in Vercel Dashboard or via CLI
# - SQLite won't work reliably on Vercel (use Vercel Postgres)
# - File uploads need external storage (Vercel Blob or S3)

