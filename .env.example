# Flask Configuration
FLASK_ENV=production
FLASK_APP=app.py

# Security Keys (GENERATE NEW ONES - DO NOT USE THESE)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here-generate-new-one
JWT_SECRET_KEY=your-jwt-secret-key-here-generate-new-one

# Server Configuration
PORT=8080

# Optional: Database URL (if using external PostgreSQL)
# DATABASE_URL=postgresql://user:password@host:port/dbname

# Optional: Audiveris Configuration
# AUDIVERIS_PATH=/opt/audiveris

